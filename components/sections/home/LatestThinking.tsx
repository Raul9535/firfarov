import { SectionPlaceholder } from "@/components/dev/SectionPlaceholder";
import type { Locale } from "@/lib/i18n/config";

export function HomeLatestThinking(_props: { locale: Locale }) {
  return <SectionPlaceholder order={7} label="Latest thinking" />;
}
