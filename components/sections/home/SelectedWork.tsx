import { SectionPlaceholder } from "@/components/dev/SectionPlaceholder";
import type { Locale } from "@/lib/i18n/config";

export function HomeSelectedWork(_props: { locale: Locale }) {
  return <SectionPlaceholder order={3} label="Selected work" />;
}
