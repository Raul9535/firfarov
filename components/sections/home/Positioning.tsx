import { SectionPlaceholder } from "@/components/dev/SectionPlaceholder";
import type { Locale } from "@/lib/i18n/config";

export function HomePositioning(_props: { locale: Locale }) {
  return <SectionPlaceholder order={2} label="Positioning statement" />;
}
