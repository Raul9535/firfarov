import { SectionPlaceholder } from "@/components/dev/SectionPlaceholder";
import type { Locale } from "@/lib/i18n/config";

export function HomeFounderMoment(_props: { locale: Locale }) {
  return <SectionPlaceholder order={5} label="Founder moment" />;
}
