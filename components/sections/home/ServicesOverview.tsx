import { SectionPlaceholder } from "@/components/dev/SectionPlaceholder";
import type { Locale } from "@/lib/i18n/config";

export function HomeServicesOverview(_props: { locale: Locale }) {
  return <SectionPlaceholder order={4} label="Services overview" />;
}
